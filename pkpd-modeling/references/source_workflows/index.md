# Source workflows for pkpd-modeling

Select the narrowest applicable workflow. Each deployment copy is self-contained and mirrors the canonical package under `04_claude_science_skills/_source_workflows/`.

- [`pkpd-pharmacometrics`](pkpd-pharmacometrics/WORKFLOW.md) — Run a staged population pharmacometrics workflow that validates concentration-time and optional response data, diagnoses concentration-effect delay, fits population PK and optional PD models, evaluates them, and optionally performs target-gated exposure-response dose simulation.
